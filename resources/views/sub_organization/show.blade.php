@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Sub_organization {{ $sub_organization->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/sub_organization') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/sub_organization/' . $sub_organization->id . '/edit') }}" title="Edit Sub_organization"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('sub_organization' . '/' . $sub_organization->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Sub_organization" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $sub_organization->id }}</td>
                                    </tr>
                                    <tr><th> Name Sub Organization </th><td> {{ $sub_organization->name_sub_organization }} </td></tr><tr><th> Phone </th><td> {{ $sub_organization->phone }} </td></tr><tr><th> Name Partner </th><td> {{ $sub_organization->name_partner }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
