@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Cancel_Profile {{ $cancel_profile->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/cancel_-profile') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/cancel_-profile/' . $cancel_profile->id . '/edit') }}" title="Edit Cancel_Profile"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('cancel_profile' . '/' . $cancel_profile->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Cancel_Profile" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $cancel_profile->id }}</td>
                                    </tr>
                                    <tr><th> Reason </th><td> {{ $cancel_profile->reason }} </td></tr><tr><th> Reason Other </th><td> {{ $cancel_profile->reason_other }} </td></tr><tr><th> Amend </th><td> {{ $cancel_profile->amend }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
